version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install
        - node build.js
    build:
      commands:
        - echo "Build phase completed"
  artifacts:
    baseDirectory: .
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/* 